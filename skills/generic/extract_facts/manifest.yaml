id: extract_facts
name: "Extract Facts"
enabled: true
description: "Extracts facts, figures, and citations from document content"
agents: [argos]
inputs: [content, sources]
outputs: [facts, citations]
