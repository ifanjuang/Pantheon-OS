# Tests — variant_generation

Documentation-level tests only.

---

## Test 1 — bounded count

Expected:

```text
variant count is explicit
variant count defaults to 3 unless justified
```

---

## Test 2 — meaningful variants

Expected:

```text
variants differ by strategy, risk, tone or structure
not only cosmetic synonyms
```

---

## Test 3 — no final selection

Expected:

```text
variant_generation proposes candidates
selection is left to review or ZEUS arbitration when needed
```
