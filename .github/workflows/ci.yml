name: CI

on:
  push:
    branches: ["**"]
  pull_request:
    branches: [main, develop]

jobs:
  # ── Qualité du code ────────────────────────────────────────────────
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install linters
        run: pip install ruff==0.8.0

      - name: ruff check
        run: ruff check platform/api/ tests/

      - name: ruff format --check
        run: ruff format --check platform/api/ tests/

  # ── Tests ──────────────────────────────────────────────────────────
  test:
    name: Tests
    runs-on: ubuntu-latest

    services:
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_DB: pantheon_test
          POSTGRES_USER: pantheon
          POSTGRES_PASSWORD: testpassword
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U pantheon"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      DATABASE_URL: postgresql+asyncpg://pantheon:testpassword@localhost:5432/pantheon_test
      DATABASE_URL_SYNC: postgresql://pantheon:testpassword@localhost:5432/pantheon_test
      ASYNCPG_URL: postgresql://pantheon:testpassword@localhost:5432/pantheon_test
      REDIS_URL: redis://localhost:6379/0
      JWT_SECRET_KEY: test-secret-key-minimum-32-characters-long
      ADMIN_EMAIL: admin@test.fr
      ADMIN_PASSWORD: TestPassword123!
      LLM_PROVIDER: ollama
      OLLAMA_BASE_URL: http://localhost:11434
      OLLAMA_MODEL: mistral:7b
      EMBEDDING_PROVIDER: ollama
      OLLAMA_EMBEDDING_MODEL: nomic-embed-text
      EMBEDDING_DIM: 768
      MINIO_ENDPOINT: localhost:9000
      MINIO_ROOT_USER: pantheon
      MINIO_ROOT_PASSWORD: testpassword123
      MINIO_BUCKET: pantheon-test
      DEBUG: "true"
      MODULES_DIR: /home/runner/work/Pantheon-OS/Pantheon-OS/modules

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install dependencies
        working-directory: platform/api
        run: pip install -r requirements.txt pytest-cov==6.0.0

      - name: Run tests
        working-directory: platform/api
        env:
          PYTHONPATH: .
        run: |
          pytest ../tests/ -q \
            --cov=. \
            --cov-report=term-missing \
            --cov-report=xml \
            --cov-fail-under=30 \
            -x

      - name: Upload coverage
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-report
          path: platform/api/coverage.xml

  # ── Sécurité dépendances ───────────────────────────────────────────
  security:
    name: Security audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install pip-audit
        run: pip install pip-audit==2.7.3

      - name: Audit dependencies
        working-directory: platform/api
        # GHSA-jr27-m4p2-rc6r: pyasn1<0.5.0 pulled by python-jose 3.4.0 (latest);
        # cannot upgrade pyasn1 without replacing the JWT library.
        run: pip-audit -r requirements.txt --ignore-vuln GHSA-jr27-m4p2-rc6r

  # ── Évaluation agents (OpenClaw) ───────────────────────────────────
  # Activé uniquement sur main/develop pour éviter la surconsommation LLM
  eval:
    name: OpenClaw regression
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop'
    needs: [test]

    services:
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_DB: pantheon_test
          POSTGRES_USER: pantheon
          POSTGRES_PASSWORD: testpassword
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U pantheon"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379

    env:
      DATABASE_URL: postgresql+asyncpg://pantheon:testpassword@localhost:5432/pantheon_test
      DATABASE_URL_SYNC: postgresql://pantheon:testpassword@localhost:5432/pantheon_test
      ASYNCPG_URL: postgresql://pantheon:testpassword@localhost:5432/pantheon_test
      REDIS_URL: redis://localhost:6379/0
      JWT_SECRET_KEY: test-secret-key-minimum-32-characters-long
      ADMIN_EMAIL: admin@test.fr
      ADMIN_PASSWORD: TestPassword123!
      LLM_PROVIDER: ollama
      OLLAMA_BASE_URL: http://localhost:11434
      OLLAMA_MODEL: mistral:7b
      EMBEDDING_PROVIDER: ollama
      EMBEDDING_DIM: 768
      MINIO_ENDPOINT: localhost:9000
      MINIO_ROOT_USER: pantheon
      MINIO_ROOT_PASSWORD: testpassword123
      MINIO_BUCKET: pantheon-test
      DEBUG: "true"
      MODULES_DIR: /home/runner/work/Pantheon-OS/Pantheon-OS/modules

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install dependencies
        working-directory: platform/api
        run: pip install -r requirements.txt

      - name: OpenClaw — dry-run (validation datasets)
        working-directory: platform/api
        run: |
          python -m apps.evaluation.cli run openclaw-chantier --dry-run
          python -m apps.evaluation.cli run openclaw-litige --dry-run
          python -m apps.evaluation.cli run openclaw-devis --dry-run
          python -m apps.evaluation.cli run openclaw-courrier --dry-run
          python -m apps.evaluation.cli run openclaw-photo --dry-run

      - name: OpenClaw — rapport JSON
        working-directory: platform/api
        run: |
          python -m apps.evaluation.cli run openclaw-chantier --json > /tmp/eval-chantier.json
          python -m apps.evaluation.cli run openclaw-litige --json > /tmp/eval-litige.json
        continue-on-error: true  # Ne bloque pas tant que le LLM est mocké en CI

      - name: Upload eval reports
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: openclaw-reports
          path: /tmp/eval-*.json
